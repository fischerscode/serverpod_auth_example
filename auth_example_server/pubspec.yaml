name: auth_example_server
description: Starting point for a Serverpod server.
publish_to: none
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  serverpod: ^1.0.1

dev_dependencies:
  lints: ^2.0.0
  auth_example_client:
    path: ../auth_example_client
  test: ^1.23.1
